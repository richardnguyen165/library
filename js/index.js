const myLibrary = [];

function Book(title, author, pages, publishYear, genre, read = false) {
  // the constructor...
  this.title = title;
  this.author = author;
  this.pages = pages;
  this.publishYear = publishYear;
  this.genre = genre;
  this.read = read;
}

function addBookToLibrary() {
  // take params, create a book then store it in the array
}

function displayBooks() {
  
}
